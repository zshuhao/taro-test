export const setToken = (value) => {
    return {
        type: 'setToken',
        value
    }
}

export const setUserInfo = (value) => {
    return {
        type: 'setUserInfo',
        value
    }
}